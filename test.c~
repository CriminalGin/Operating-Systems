#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(int argc, char *argv[]){
	int flags, opt;
	int nsecs, tfnd;
	nsecs = 0, tfnd = 0, flags = 0;
	while((opt = getopt(argc, argv, "nt:")) != -1){
		switch()

	}
}
